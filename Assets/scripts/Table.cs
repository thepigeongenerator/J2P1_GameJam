using UnityEngine;
public class Table : MonoBehaviour
{
    // checks whether the position falls on the table
    public bool IsOnTable(Vector2 pos)
    {
        // currently just assume false. implementing this later
        return false;
    }
}